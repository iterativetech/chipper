import { Config } from "./config";

export const DEFAULT_CONFIG: Config = {
    available_parts: [],
    enforced_parts: [],
    grid_height: 0,
    grid_width: 0,
    hazards: [],
    rules: 'There are no rules!',
    level: 0
  }